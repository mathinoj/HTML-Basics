function name(matt) {
    console.log("Yo " + matt);
}

function nameAgain(name) {
    console.log("YoYo " + name);
}
function rant(message) {
    console.log(message.toUpperCase());
    console.log(message.toUpperCase());
    console.log(message.toUpperCase());
}
